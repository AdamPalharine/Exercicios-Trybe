const contando = document.querySelector('#contador')

  contando.addEventListener('click', function contadoClick() {
    const paragrafo = textArea.value.length;
    contando.innerText = 500 - paragrafo;
  });

/* Eu não sei oq estou fazendo */